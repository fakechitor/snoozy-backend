spring:
  application:
    name: snoozy-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: ${AUTH_URI}
              predicates:
                - Path=/api/v1/auth/**